---
import type { CollectionEntry } from "astro:content";
import { slugifyStr } from "@/utils/slugify";
import { getPath } from "@/utils/getPath";
import Datetime from "./Datetime.astro";

type Props = {
  variant?: "h2" | "h3";
} & CollectionEntry<"blog">;

const { variant: Heading = "h2", id, data, filePath } = Astro.props;

const { title, description, ...props } = data;
---

<li class="border-b border-border py-7 first:pt-1 last:border-b-0">
  <a
    href={getPath(id, filePath)}
    class:list={[
      "inline-block text-xl font-semibold tracking-tight text-foreground sm:text-2xl",
      "decoration-2 underline-offset-4 hover:text-accent hover:underline",
      "focus-visible:no-underline focus-visible:underline-offset-0",
    ]}
  >
    <Heading
      style={{
        viewTransitionName: slugifyStr(
          title.replaceAll(".", "-").replaceAll(":", "-")
        ),
      }}
    >
      {title}
    </Heading>
  </a>
  <Datetime {...props} class="mt-2" />
  <p class="mt-3 max-w-[68ch] leading-7 text-subtle">{description}</p>
</li>
